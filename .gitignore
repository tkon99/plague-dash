# Runtime data (per-run, don't commit)
data/plague-dash.jsonl
data/countries.jsonl
data/meta.json

# Keep the samples + generator in version control
!data/plague-dash.sample.jsonl
!data/countries.sample.jsonl
!data/gen_sample.py

# Python
__pycache__/
*.pyc

# Build artifacts
mod/bin/
mod/obj/
mod/*.user
mod/.vs/
[Bb]in/
[Oo]bj/

# Packaged mod release zips (uploaded to GitHub Releases, not committed)
*.zip
dist/

# Local tooling / huge extracted reference assemblies (mod/tools/net48ref)
.zcode/
mod/tools/net48ref/

# Run exports (one-off analysis artifacts)
data/*-export.json
